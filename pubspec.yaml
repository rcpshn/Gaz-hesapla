name: my_flutter_app

description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  hive: ^2.0.0
  path_provider: ^2.0.0
  intl: ^0.18.0
  google_fonts: ^2.1.0

flutter:
  uses-material-design: true